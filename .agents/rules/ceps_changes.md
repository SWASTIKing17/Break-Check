---
trigger: always_on
---

# Code Modification Restrictions

**STRICT RULE: NEVER MODIFY THE `plugins` FOLDER**
- **NEVER** make any changes, edits, or modifications to the `plugins/` directory.
- The `plugins/` directory represents the latest stable versions and is **strictly managed manually by the user**.
- ALL development, modifications, and updates for extensions MUST be made exclusively in the `CEPs/` directory instead.